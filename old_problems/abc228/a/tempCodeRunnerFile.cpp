
  // if (S == 0) S = 24; 
  // if (T == 0) T = 24;
  // if (X == 0) X = 24;
  // if (S < T) {
  //   if ((S <= X) && (X < T)) check = 1;
  // } else {
  //   if ((X < S) && (T <= X)) check = 1;
  // }
